# ==========================================
# 1. Agentic Orchestration & LLM Backends
# ==========================================
langgraph>=1.2.0              # Supports type-safe streaming channels & node-level error handlers
langchain>=1.3.0              # Core meta-package with unified stream event protocols
langchain-core>=0.3.0         # Modern runnables and base message definitions
langchain-community>=0.3.0    # Required for document loaders, text splitters, and vector store wrappers
langchain-openai>=1.2.0       # Optimized for newer structured outputs and tooling
langchain-google-genai>=1.0.0
langchain-anthropic>=1.0.0

# ==========================================
# 2. Vector Storage & Knowledge Processing (RAG)
# ==========================================
chromadb>=0.5.0               # Local persistent database for raw CBA rule embedding chunks
pypdf>=5.1.0                  # Ingestion engine for unstructured PDF rules documentation

# ==========================================
# 3. Data Engineering & Macro Analytics
# ==========================================
nba-api>=1.5.0                # Fixed underscore to hyphen for clean package resolution
pandas>=2.2.0                 # High-performance dataframe management for metrics data
numpy>=1.26.0                 # Vectorized linear algebra operations
pydantic>=2.10.0              # Strict data-layer schema validation for state variables
scikit-learn>=1.4.0           # Regression and driver modeling for macro win-determinants

# ==========================================
# 4. Utilities & Environment Configuration
# ==========================================
pyyaml>=6.0.1                 # Ingestion parsing for settings.yaml file
python-dotenv>=1.0.1          # Safely manages multi-provider API keys
requests>=2.31.0              # Under-the-hood HTTP payloads handling